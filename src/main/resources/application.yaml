spring:
  application:
    name: crew-service

  datasource:
    url: jdbc:mysql://localhost:3306/crew_service?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: root
    password: dlqudrb209@@
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

logging:
  level:
    org.hibernate.SQL: debug